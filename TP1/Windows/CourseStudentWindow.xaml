<Window x:Class="TP1.CourseStudentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cours (Étudiant)"
        Height="520" Width="760"
        WindowStartupLocation="CenterScreen"
        Background="#212529">
    <Window.Resources>
        <SolidColorBrush x:Key="Card" Color="#2B3035"/>
        <SolidColorBrush x:Key="Text" Color="#E9ECEF"/>
        <SolidColorBrush x:Key="Border" Color="#3A3F44"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="PillTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="{StaticResource Card}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="18" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PanelCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Card}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="ListCard" TargetType="ListBox">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="{StaticResource Card}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Id :" />
        <TextBox x:Name="txtId" Grid.Row="0" Grid.Column="1" Style="{StaticResource PillTextBox}" IsReadOnly="True" Width="240"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Last Name :" Margin="0,10,8,0"/>
        <TextBox x:Name="txtLastName" Grid.Row="1" Grid.Column="1" Style="{StaticResource PillTextBox}" IsReadOnly="True" Width="260" Margin="0,10,0,0"/>

        <Label Grid.Row="1" Grid.Column="2" Content="First Name :" Margin="16,10,8,0"/>
        <TextBox x:Name="txtFirstName" Grid.Row="1" Grid.Column="3" Style="{StaticResource PillTextBox}" IsReadOnly="True" Width="260" Margin="0,10,0,0"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" Text="Results :" FontWeight="Bold" Margin="0,14,0,6"/>

        <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4" Style="{StaticResource PanelCard}">
            <ListBox x:Name="lstResults" Style="{StaticResource ListCard}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding EvalName}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding StudentId}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding StudentFullName}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding Result}"/>
                            <Run Text=" / "/>
                            <Run Text="{Binding Value}"/>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Label Grid.Row="4" Grid.Column="0" Content="Total :" Margin="0,10,8,0"/>
        <TextBox x:Name="txtTotal" Grid.Row="4" Grid.Column="1" Style="{StaticResource PillTextBox}" IsReadOnly="True" Width="240" Margin="0,10,0,0"/>

        <Label Grid.Row="4" Grid.Column="2" Content="Percentage :" Margin="16,10,8,0"/>
        <TextBox x:Name="txtPercent" Grid.Row="4" Grid.Column="3" Style="{StaticResource PillTextBox}" IsReadOnly="True" Width="240" Margin="0,10,0,0"/>
    </Grid>
</Window>
